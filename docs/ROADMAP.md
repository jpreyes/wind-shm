# PÓRTICO — Roadmap de mejoras

Plan de mejoras detectadas en uso práctico (análisis y diseño), agrupadas por
similaridad. `[#]` referencia el pedido original. Estado: ⬜ pendiente · 🟡 en curso · ✅ hecho.

---

## G1 · Panel de análisis y acceso a resultados ✅
*El cuello de botella del flujo: lanzar análisis y reusar resultados.*
- ✅ **Ventana flotante de análisis** (Centro de análisis): el botón "Análisis" de la barra lateral abre un panel con TODOS los análisis (Estático, Modal, Espectro + 6 avanzados NL-lite), cada uno con botón Ejecutar. `[#4]`
- ✅ **Acceso a resultados ya corridos**: badges ✓/sin-ejecutar por análisis y botón **Ver** que re-muestra sin recalcular (estático, modal, y cada caso espectral listado). **Indicador permanente** en la barra de estado (`#sb-results`): resume qué análisis tienen resultados (✓ Estático · Modal · N esp · Pandeo), en verde, y abre el hub al clic; se actualiza tras correr/limpiar cualquier análisis. `[#1]`
- ✅ **Analizar seleccionados (batch)**: casilla por análisis en el Centro de análisis + botón **Analizar seleccionados** (`_runBatch`) que corre la secuencia marcada en orden lógico (estático → modal → espectro → NL-lite); el espectro se antepone con modal si falta. `runAnalysis`/`runSpectrum` ahora devuelven promesa (se envuelve el `setTimeout`) para esperar fin antes del siguiente. `[#24]`
- ✅ **Modal/progreso**: el modal **sale del modo resultados antes de correr**; la **estructura original** se dibuja como **fantasma tenue** (0.28); la **caja flotante de progreso** aparece en estático, **Modal**, **Espectro** y los NL-lite síncronos sin diálogo (No lineal / P-Delta / Pandeo, vía `_runByAction` con yield). Los NL-lite con diálogo (form-finding/plástico/pushover-DC) gestionan su propio flujo. `[#2]`
- ✅ **Auto-discretización para los análisis de pórtico**: un helper compartido `_applyAutoDiscIfEnabled()` aplica/revierte la malla ×N igual que el estático; lo usan ahora **modal**, **pandeo**, **P-Delta** y **rótulas** (sin selección). El **espectro** la hereda del modal. Se **excluyen** a propósito los reticulados (No lineal/cables y pushover-DC, donde los nodos internos de una barra subdividida quedarían sin rigidez transversal → mecanismo) y el **form-finding** (edita geometría/selección). `[#36]`
- ✅ **Indicadores de resultados para los NL-lite** en el Centro de análisis: badge ✓/sin-ejecutar y botón **Ver** por cada avanzado (No lineal, P-Delta, Pandeo, Rótulas, Pushover) que reabre su overlay; `_nlResult/_pdResult` se limpian al limpiar/cambiar de modelo. Form-finding no deja resultado reutilizable (reposiciona nodos), por eso no lleva badge. `[#37]`
- ✅ **«Analizar seleccionados» sin diálogos**: el lote corre cada análisis con sus **parámetros por defecto** vía `opts.silent` (nº de modos, método, Mp, imperfección, q…). El espectro reutiliza la última curva definida. La ejecución individual sigue mostrando su diálogo. `[#38]`
- ✅ **Persistir los parámetros de ejecución en el `.s3d`**: `_gatherAnalysisParams/_applyAnalysisParams` guardan y restauran nº de modos, método modal, modos de pandeo, Mp, pasos NL y auto-disc; se embeben en el archivo vía `_fullSaveJSON` (NO en `serializer.toJSON`, que alimenta `_modelSig`, para no invalidar la caché de resultados al variar). `[#39]`
- ✅ **Hub de análisis → sub-pestañas**: "Estático/lineal" y "Avanzados (NL-lite)" en dos sub-pestañas dentro del Centro de análisis. `[#40]`

## G2 · Motor modal y rendimiento ✅
- ✅ **Método modal alternativo + selector**: además de la iteración inversa (Stodola, modo a modo), nueva **iteración de subespacio (Bathe)** que extrae los modos menores en bloque (rápida con muchos modos), con un eigensolver generalizado pequeño (Cholesky + Jacobi). Selector en la ventana modal. Verificado: subspace ≡ Stodola en frecuencias (portal 3D: 6.21, 6.21, 7.02, 9.94 Hz idénticas) y eigensolver pequeño exacto vs solución analítica. `[#3]`

## G3 · Navegación y legibilidad del viewport *(quick wins)* ✅
- ✅ **PAN (manito)** además de orbitar. Herramienta en la barra lateral (modo `pan`): arrastrar con la izquierda panea, restaura orbit al salir. `[#8]`
- ✅ **Grillas más tenues** (opacidad 0.7→0.38) → los elementos resaltan. `[#9]`
- ✅ **Ocultar los ejes**: Vista → Ejes XYZ (`toggleAxes`). `[#10]`

## G4 · Modelado e interacción de edición ✅ *(completo)*
- ✅ **Crear elementos sin nodos previos + imán a nodos cercanos (toggle).** En modo Elemento, clic en la grilla crea el nodo; con Imán (casilla en la barra superior, por defecto ON) el extremo se pega al nodo cercano (lo reutiliza); apagando el imán crea uno nuevo aunque haya otro al lado. `[#6]`
- ✅ **Herramienta "Área" en la barra lateral** (Nodo/Elem/**Área**/Apoyo): clic en 3 (CST) o 4 (QUAD) nodos; el 4º crea el QUAD, Enter crea el CST, Esc reinicia. Usa las últimas opciones (espesor/comportamiento) y se ajustan luego en el panel del área. `[#nuevo]`
- ✅ **Acciones + Mover/Copiar con un solo elemento** seleccionado (antes solo con multi-selección). `[#7]`
- ✅ **Copiar elemento = copiar también sus cargas (dist/temp), cable/L0, y grupos.** `[#11]`
- ✅ **Nodo + elemento en un solo clic**: el clic crea el nodo (si hace falta) y lo deja como extremo inicial sin un segundo clic; tras cerrar un tramo, el extremo recién creado inicia el siguiente → **cadena continua** (poligonal con un clic por tramo, Esc para terminar). `[#26]`
- ✅ **Fuerza nodal a multi-selección**: formulario de carga nodal (Fx..Mz) en el panel de selección múltiple de nodos + `setCargaNodalSelected(F, lcId)` que la asigna idéntica a todos los nodos seleccionados (y "Limpiar todos"). `[#32]`
- ✅ **Limpiar cargas huérfanas**: `model.removeNode`/`removeElement` purgan de todos los casos las cargas (nodales / dist / temp) que referencian lo borrado (`_purgeNodeLoads`/`_purgeElemLoads`); los `delete*` de la app llaman `refreshLoads()` → las flechas desaparecen de la vista y del modelo. `[#31]`

## G5 · Cargas, normativa y asistente de modificación ✅
- ✅ **Casos de carga y combinaciones de la norma por defecto**: Análisis → "Crear casos y combos de norma (NCh3171)" → casos D (PP) y L, combos 1.4D y 1.2D+1.6L, y sísmicas ±1.4Ex/±1.4Ey si existen casos espectrales. Editables, idempotente (`crearCasosYCombosNorma`). `[#16]`
- ✅ **Asistente sobre el modelo ya construido (#5)**: pestaña Asistente → "Modificar el modelo actual": orden en lenguaje natural → el Worker/LLM la traduce a **operaciones** (`/api/asistente/modificar`) y un ejecutor determinista cliente (`js/model/model_ops.js`, `aplicarOperacionesModelo`) las aplica. Operaciones: `add_load` (cargas viva/uniforme/trapecial a selección/vigas/columnas), `add_story` (anexar piso encima), `add_bay` (anexar vano lateral ±x/±y), `set_modifiers` (factores de rigidez, sección clonada), `set_mass` (masa nodal). Verificado: pórtico → 2 pisos × 2 vanos resuelve con ΣRz = carga total (equilibrio); apoyos heredados en la base del vano nuevo evitan el mecanismo. `[#5]`
- ✅ **Asistente en la barra lateral derecha**: pestaña **Asistente** junto a Modelo/Resultados/Diseño (`vpanel-asistente`: generar modelo + modificar modelo). `[#23]`
- ✅ **Combos de servicio / tensiones admisibles**: `crearCasosYCombosNorma` agrega, además del LRFD, el set ASD (NCh3171·ASCE-7): D, D+L y por dirección sísmica D±0.7E, D+0.75L±0.525E, 0.6D±0.7E. `[#25a]`
- ✅ **Creación automática de combos accesible en dos lugares**: menú Análisis **y** botón "⚙ Crear casos y combos de norma" en la pestaña Combos de la barra lateral (`#btn-combos-norma`). `[#25b]`
- ✅ **Cargas trapeciales (trapezoidales)** en elementos: campo "w en j" en el panel del elemento (vacío = uniforme). FEF exacta (uniforme + triangular), diagramas V(x)/M(x) con q lineal y extremo por cuadrática, interpolación correcta al discretizar (auto-disc) y al unir/partir, round-trip JSON y CSV (w2 opcional). Verificado: viga SS con carga triangular → reacciones w₀L/6 y w₀L/3 exactas, M_max=w₀L²/(9√3)=10.264 en x=L/√3, idéntico con 1/4/10 sub-elementos. `[#35]`

## G6 · Diseño, memoria y reportes ✅
- ✅ **Tabla de diseño explorable**: wrapper con scroll (max-height 58vh) hasta el último elemento + columna **|δ| mm** (desplazamiento máx. de los nodos del elemento en el caso/combo mostrado). `[#12]`
- ✅ **Memoria de cálculo descargable en `.docx`**: generador Word **autocontenido** (`js/io/docx.js`, sin dependencias ni build — ZIP STORED + CRC32 + WordprocessingML, con encabezados, tablas e imágenes PNG embebidas). `app.generarMemoriaDocx` arma la misma memoria (portada, bases, materiales/secciones, cargas/combos, modal, figuras, D/C, flechas, derivas, limitaciones) y la descarga. Menú Análisis → "📝 Memoria de Cálculo (Word .docx)". Verificado: ZIP/XML bien formados (`test_docx.mjs`) e imágenes embebidas en navegador. `[#14]`
- ✅ **Quitar logos UACh de la memoria** cuando se cargue el logo profesional: con token PRO + logo de empresa cargado, ese logo **reemplaza** a los académicos (UACh/Facultad/IOC) y oculta el badge "Producto académico" en portada (HTML y `.docx`). Sin logo PRO se conservan los créditos. `[#18]`
- ✅ **Portada/landing flotante al entrar** (`#landing`): ventana flotante compacta con logo **PÓRTICO**, logos institucionales (UACh/Facultad/IOC), autor (Dr. Juan Patricio Reyes C.) y botones **Entrar** / **Generar modelo con el asistente** / **Manual de uso** / **Instalar app**; en pantallas bajas se apaisa (2 columnas). `[#22]`
- ✅ **«Generar modelo con el asistente» de la portada abre el diálogo "generar modelo desde ficha"** (`asistenteDialog`), coordinado con el arranque (flag honrado al terminar `_restoreOrLoadExample`, tras resolver el "¿recuperar sesión?") para no chocar con el modal de inicio. `[#22b]`
- ✅ **Quitar de la UI/ayuda referencias a editar archivos de config**: reformuladas las menciones a editar `asistente/diseno_params.json` → "valores normativos estándar". `[#13]`
- ✅ **Memoria independiente por proyecto (modelo)** `[#41]`: la memoria (título, kicker, institución, proyectista, revisor, descripción, pie, limitaciones, flags de figuras) pasa a ser **por proyecto** en `model.memoria` y viaja en el `.s3d`. `_memoria()` devuelve la **efectiva** = defaults globales (config) sobrescritos por los del modelo; `configDialog` edita la del modelo y refleja un default global para modelos nuevos; los generadores HTML/`.docx` usan `_memoria()`. El logo de empresa sigue global (branding del despacho). Compatible con archivos viejos (sin `memoria` → defaults). Verificado round-trip y que editar la memoria **no** invalida la caché de resultados.

## G7 · Gestión de proyecto multi-modelo
*Decisión tomada: los modelos se integran **sólo en la memoria** (una sola memoria de cálculo), **sin** vínculo geométrico entre ellos.*
- ⬜ **Un proyecto con varios modelos** (edificio principal, cercha plana, viga de fundación…) que se integran en **una sola memoria**, cada uno como una **sección/capítulo** del documento; no se comparten ni vinculan geometrías. `[#17]` *(cambio más arquitectónico: serializer guarda una lista de modelos por proyecto, estado de la app con modelo activo, generador de memoria que recorre todos).*

## G8 · Robustez y diagnóstico ✅
- ✅ **Diagnóstico de inestabilidades**: `diagnoseInstability()` detecta los GDL libres con rigidez nula (diagonal de K ≈ 0) → nodo/GDL culpable. `runStabilityDiagnosis()` (menú Análisis → "Diagnosticar estabilidad") los **resalta en rojo, agranda y centra la vista**; se invoca **automáticamente** cuando un análisis falla por singular/mecanismo. Verificado: nodo aislado "invisible" detectado con sus 6 GDL y resaltado. `[#15]` *(Nota: cubre el caso común de rigidez nula; mecanismos multi-GDL acoplados se avisan pero no se localizan.)*
- ✅ **CRÍTICO — pérdida irreversible del modelo** `[#42]`: el riesgo venía de que la auto-discretización **reemplaza** `this.model` por la malla subdividida (original en `_predisc`) y "Limpiar resultados" revertía a ese `_predisc`, que podía estar **desfasado** (tras editar) o ser de **otro modelo** (no se limpiaba al abrir/cargar). Solución: (a) `snapshot()` — el **primer edit** sale de la malla auto-disc volviendo al modelo ORIGINAL antes de mutar (nunca se edita/guarda la malla); (b) `clearResults()` empuja el modelo al **undo** antes de revertir (Ctrl+Z lo recupera) y limpia `_plasticResult/_dcResult/_buckResult`; (c) `_loadJSON()` resetea `_predisc` y los resultados NL/modal/espectro al cambiar de modelo. Verificado en navegador. *(Pendiente aparte: confirmación/respaldo antes de un Guardar que sobrescriba.)*
- ✅ **Autoguardado periódico + recuperación múltiple** `[#43]`: cada **sesión** autoguarda en su propio slot (`portico_autosave_<sid>`) + un índice (`portico_autosaves`) con los **6 modelos más recientes**; autoguardado **cada 5 min** (además del debounced a 1.5 s y el `beforeunload`), tolerante a cierre/corte de luz durante ediciones largas. La portada muestra un **diálogo que lista** los autoguardados (nombre, fecha, nº nodos/elem.) para **reanudar cualquiera**, eliminar uno o empezar nuevo. No autoguarda el modelo vacío. Compatible con la clave única antigua. Verificado en navegador.

## G9 · Verificación documentada y documentación 🟡 *(en curso — pipeline + 3 casos)*
*Decisión tomada: priorizar los casos en este orden → **análisis dinámico modal → viga → pórtico → muro/shell**.*
- 🟡 **Casos de la literatura SAP2000 y Sofistik** (en `referencias/`) → convertirlos a formato Pórtico, **comparar/verificar y documentar**; quedan en **Ejemplos** como casos de verificación. **Orden de prioridad (decisión tomada): (1) análisis dinámico modal, (2) viga, (3) pórtico, (4) muro/shell.** `[#19]`
  - **Pipeline batcheado** (`tools/run_verifs.mjs`): corre el **solver real de Pórtico headless** (Node) → **figura 3D SVG** (sin navegador) → **comparador** → **`.md` + `.pdf`** con membrete IOC (`tools/md2pdf.mjs`, Chrome/Edge headless). Manifiesto por caso en `tools/verif/cases/`. Lo único caso-a-caso: construir el `.s3d` y validar.
  - **Figuras de los PDF legibles**: `poppler` (`pdftoppm`) instalado en `%LOCALAPPDATA%\poppler` → se renderiza y **lee** la geometría de cualquier caso (clave para los pórticos irregulares).
  - **Casos hechos**: ✅ **1-014** (modal voladizo, 0.024 %) · ✅ **1-021** (modal Bathe-Wilson 10×9) · ✅ **1-018** (estático flexión+corte+axial, **0.000 %**) · ✅ **1-005** (asentamiento de apoyo `[#54]`, **0.000 %**) · ✅ **3-002** (viga plane-stress `[#58]`: axial exacto + QUAD ≡ elemento plano SAP) · ✅ **3-004** (cilindro plane-strain `[#58]`, −0.9 % vs Timoshenko) · ✅ **2-014** (gradiente térmico en placa `[#57]`, U_z −1.3 % / R₂ −2.8 %) · ✅ **1-012** (no-tension/no-compression `[#56]`: 3 modelos cable/puntal, ≤0.16 %) · ✅ **1-017** (cuerda tensa, modal con Kg `[#55]`, **≤0.02 %**) · ✅ **1-031** (etapas constructivas `[#59]`, **0.000 %**) · ✅ **1-009** (pretensado por tendón `[#60]`, ≤0.011 %) · ✅ **1-030** (cargas móviles / líneas de influencia `[#61]`, **0.000 %**).
  - **Runner ampliado**: multi-caso estático (`spec.lcIds`, usado por 3-002) · **NL-lite** (`nllite`, reticulado cable/puntal, 1-012) · **modal con Kg** (`modalKg`, 1-017) · **etapas** (`staged`, 1-031) · **pretensado** (`tendon`, 1-009) · **cargas móviles** (`moving`, 1-030). Con eso, las 5 capacidades de G14 + los **3 motores de puente** (#59/#60/#61) quedan verificados contra solución analítica / manual CSI.
  - **Mapas de cobertura**: [`docs/verificaciones-disponibles.md`](verificaciones-disponibles.md) (qué caso verifica qué, + ejemplos de **diseño** reusables sólo para análisis + edificios/puentes) y [`docs/capacidades-portico.md`](capacidades-portico.md) (capacidades completas/parciales/ausentes).
- ⬜ **Mejorar UX de los análisis avanzados** (no lineales) + **ejemplo sencillo y `.md` por funcionalidad** (pandeo, form-finding, pushover). `[#20]` *(detalle técnico desglosado en G11.)*
- ⬜ **Documentación integral de toda funcionalidad**: qué hace, teoría mínima, cómo ejecutarla en la app, ejemplo caracterìstico. `[#21]`

## G10 · Completar la física de elementos (FEM / shell) ✅ *(postproceso de áreas resuelto)*
*Continuación del trabajo de placa/shell.*
- ✅ **Contorno de tensiones de flexión de placa**: `plateMoments` (momentos Mx,My,Mxy en el centro, MITC4 y DKT) → tensión de superficie `σ=±6M/t²` → von Mises de **envolvente** max(cara sup, cara inf) en `getAreaStress` (`areaBendingStress`); el contorno y el suavizado nodal usan la envolvente para shells. Panel del área muestra vM superficie/membrana/sup/inf. Verificado: momento central placa SS quad −1.6% / tri −2.6% vs 0.0479·q·a²; contorno de voladizo shell flexión-dominado.
- ✅ **Torsión de St. Venant**: el `J` ya se auto-calcula de la geometría en todas las secciones paramétricas (rect, circular, huecas; IPE/HEB tabuladas). Mejorado: fórmula rectangular a la serie precisa de Roark `J=a·t³·[1/3−0.21(t/a)(1−(t/a)⁴/12)]`; corregido el `J` de la sección por defecto 30×30 (era 1.13e-4, 10× bajo → 1.14e-3).
- ✅ **Masa de área para el modal**: las áreas aportan `ρ·t·A` (lumped, repartida a los GDL de traslación) a la matriz de masas, en el ensamblaje denso y disperso (`assembleAreasMassInto`). Verificado: masa total por dirección = ρ·t·A.
- ✅ **Postproceso de elementos de área (shell/placa/membrana)** `[#49]`: (a) nueva opción **«Tensiones áreas (von Mises)»** en `#result-type` (atajo `8` y menú Vista), que se **muestra sólo si el modelo tiene áreas** (`_syncResultTypeOptions`) y dibuja el contorno suavizado nodal vía `viewport.showAreaStress`→`colorAreasByVM`; (b) en la **deformada** las caras ahora se mueven con los desplazamientos nodales (`colorAreasByVM(results, scale)` con `getDeformedCoords`); (c) al **seleccionar un área** (también con clic en modo Resultados, que ahora enruta `areaHits`→`panel.showArea`) el panel muestra el par completo **tensiones + deformaciones**: tensiones (vM superficie/membrana/sup/inf, σ₁/σ₂), **deformaciones unitarias** de membrana (εx,εy,γxy + ε₁/ε₂, vía `areaStrain`), **momentos de placa** (Mx,My,Mxy = σ_sup·t²/6), **curvaturas de flexión** (κx,κy,κxy, vía `areaCurvature`/`plateCurvatures`) y **desplazamientos nodales**; (d) **bug raíz**: un modelo **sólo de áreas** (muro/losa) era rechazado por la guarda `elements.size===0` en `runAnalysis`/`runModal` → ahora acepta barras **o** áreas. Verificado en `test_area_post.mjs` (membrana uniaxial: σx=F/(b·t) y εx=σx/E exactos, εy=−ν·σx/E; momentos y curvaturas de placa no nulos; ΣReacciones=ΣCargas) y en el navegador (contorno y deformada de un voladizo shell 4×2; panel con los 5 bloques).

## G11 · Análisis avanzados: corrección, rendimiento y UX 🟡 *(reabierto — bugs en producción)*
*Cluster de incidencias detectadas usando los análisis no lineales en producción.*
- ✅ **Rótulas plásticas — panel en la barra derecha**: los resultados pasan de la ventana flotante (que usaba `var(--panel,#0f1830)` inexistente → fondo oscuro fijo + texto de tema = ilegible en claro) a una **pestaña «Rótulas» en Resultados** (`rtab-plastico`, `renderPlasticResults`), que respeta el tema por construcción. De paso se corrigió el mismo bug de tema en los overlays de pandeo, P-Delta y pushover-DC (→ `var(--bg4)`/`--border`/`--text`). `[#27a]`
- ✅ **Rótulas plásticas — selección por elemento**: el diálogo (`_plasticDialog`) permite un Mp por defecto y un **Mp distinto para los elementos seleccionados**, con opción **«sólo la selección rotula»** (el resto permanece elástico → capacidad ∞). El bucle usa la capacidad por elemento (`capByElem`) y la tabla muestra el Mp de cada rótula. `[#27b]`
- ✅ **Rótulas plásticas — 3 comportamientos + N/V/M + curva constitutiva**: el diálogo ofrece **dúctil (perfectamente plástica, meseta ∞)**, **dúctil con caída** (meseta hasta una deformación última θu (rotación) / δu (traslación) y luego cae a un residual) y **frágil** (caída inmediata). La caída redistribuye y puede gatillar **colapso en cascada** al mismo λ (↯). Además del momento (My/Mz), se pueden dar capacidades de **axial Np** y **cortante Vp** → rótulas/​**resortes no lineales** de N y V con los mismos 3 comportamientos. Se dibuja la **curva constitutiva** (backbone N/V/M) en el diálogo (en vivo) y en resultados. Verificado: portal 2D → perfecto λc=3.64 (4 rótulas) · frágil λc=2.61 (cascada) · dúctil-con-caída como continuo entre ambos (θu→∞ ≡ perfecto, θu→0 ≡ frágil); rótula **axial** dispara a Np.
- ✅ **Pandeo lineal — motor rápido tipo modal**: nueva **iteración de subespacio** para `(K + λKg)φ = 0` (`js/solver/buckling.js` + `buckling_worker.js`), reusando el núcleo extraído del modal (`js/solver/subspace.js`: `smallGenEig` + helpers de banda). Reduce con Cholesky sobre Kᵣ (SPD) ya que −Kg es indefinida; los |1/λ| dominantes dan los menores λcr. Verificado equivalente a **Euler** (columna biarticulada, `test_buckling.mjs` y en navegador): λ₁ −0.28%, λ₂ −1.1%, pares degenerados. Reemplaza el `numeric.eig` denso O(n³) que se colgaba. `[#28][#33a]`
- ✅ **Pandeo lineal — UI análoga a la modal**: `runBuckling` ahora **asíncrono**, con diálogo de N° de modos (`_buckNModesDialog`), **caja de progreso** y el eigensolver en **Web Worker** (no bloquea la UI). Resultados en el overlay: selector de **modo de pandeo + λcr**, escala y **carga de pandeo por elemento** (`N_cr = λcr·N_ref`, barras más comprimidas; `assembleKg` devuelve el axial por elemento `Nby`). `[#33b]`
- ✅ **Form-finding — bug de geometría**: `runFormFinding` ahora **acota la red a los elementos seleccionados** (los demás quedan fijos; los nodos frontera con estructura no participante actúan de ancla) y permite elegir **ejes a ajustar** (sólo Z por defecto, o 3D). Así formar sólo la viga **no destruye los pilares**. `formFind` acepta `axes` para no redistribuir las luces en planta. Verificado: `test_formfind.mjs` (pilares intactos, viga funicular simétrica, planta preservada; el caso "todo el modelo 3D" reproduce el colapso original → por eso se acota). `[#29]`
- ✅ **Form-finding — metodología documentada**: `docs/form-finding.md` — rol de la densidad de fuerza **Q**, anclas/objetivo y acotamiento, ejes, y el ejemplo viga cargada → funicular (y arco al invertir la carga). `[#30]`
- ✅ **Pushover — documentar ejecución**: `docs/pushover.md` — los dos pushover (control de desplazamiento geométrico y rótulas plásticas), paso a paso, qué decide el programa (GDL de control, objetivo, pasos), lectura de la curva λ–δ / secuencia de rótulas, y ejemplos (snap-through de cercha von Mises; colapso plástico de pórtico). `[#34]`
- ✅ **NL-lite: caja de progreso + motores rápidos** `[#44]`: (a) **P-Delta** pasa del solver **denso** `numeric.solve` O(n³) al factorizador en **banda** (`makeFactor`/Cholesky) ya usado por pandeo/rótulas, en el estado lineal y en cada iteración — verificado contra **Euler** (voladizo P/Pcr≈0.27 → amplificación 1.367 vs 1.366). (b) **Caja de progreso** (como modal/pandeo) en No lineal, P-Delta, Rótulas y Pushover al correrse individualmente. (c) **Offload real a Web Worker** (`js/solver/nl_worker.js`): los solvers **densos** de `nl_lite.js` (Newton corotacional `solveNonlinear` y control de desplazamiento `solveNonlinearDC`, O(n³) por iteración) corren fuera del hilo principal — **No lineal** y **Pushover** ya no congelan la UI. `_solveNLInWorker(kind, opts)` despacha al worker (con *fallback* al hilo principal si no hay module workers) y el resultado vuelve por clonado estructurado (preserva los `Float64Array` de `steps`/`path`). P-Delta y Rótulas ya usan el motor en banda (≈O(n·b²)), mucho más liviano. Verificado en navegador: ida y vuelta `nl`/`dc` con `Float64Array` intactos (barra axial: λ=1, N=100).
- ✅ **Rótulas plásticas / pushover — elegir caso o combinación de carga** `[#45]`: selector de **patrón de carga** (todos los casos / un caso / una combinación) en los diálogos de rótulas y pushover. `_resolvePattern` escala los aportes por su factor y respeta el peso propio de cada caso (p.ej. 1.2·D en un combo). La etiqueta del patrón se muestra en el resultado.
- ✅ **BUG rótulas plásticas / pushover-DC — diagnóstico engañoso** `[#46]`: la causa raíz era `[#42]` (se evaluaba un modelo equivocado/sin cargas → "nadie forma rótulas" / "no produce desplazamiento"). Corregido `[#42]` + diagnósticos claros: `runPlastic` detecta **patrón de carga nulo** (sin cargas ni peso propio) y distingue **mecanismo desde el inicio** de "ningún extremo alcanza Mp"; `runPushoverDC` aclara que idealiza las barras como **reticulado** (sólo axial/cables, sin flexión) y deriva los pórticos a flexión hacia «Rótulas plásticas». Verificado: pórtico cargado forma 4 rótulas (λc=0.371); patrón nulo → mensaje correcto. *(Falta aparte: elegir caso/combo `[#45]` y `runPushoverDC` con flexión real.)*
- ✅ **BUG resultados de rótulas no se actualizan** `[#46]`: `clearResults()` y `_loadJSON()` ahora limpian `_plasticResult`/`_dcResult` → la pestaña «Rótulas» ya no muestra la corrida anterior al limpiar o cambiar de modelo.
- ✅ **Visualizar la secuencia de formación de rótulas en el modelo** `[#47]`: la deformada plástica marca cada rótula en la posición deformada de su nodo, **coloreada por orden de formación** (gradiente **amarillo→rojo**). Panel flotante con **slider + play** para recorrer la secuencia paso a paso (rótula k/N, elemento, λ). `showNLDeformed` acepta `hinges`; el overlay/timer se limpian al limpiar resultados. Verificado: portal 2D → 4 rótulas (mecanismo, λ 0.90→1.07).
- ✅ **BUG rótulas plásticas — falso «mecanismo desde el inicio» con diafragmas** `[#50]`: `_plasticAssemble` ensamblaba K **sólo desde los elementos**, sin `applyDiaphragmConstraints` (penalti) que `assembleK` (estático/P-Delta/pandeo) sí aplica. En un modelo con diafragmas rígidos, los **nodos maestros** (sin elementos conectados) quedaban con rigidez nula → K singular → el solver reportaba «la estructura es inestable desde el inicio (mecanismo)» aunque el estático corría perfecto. Corregido: el ensamble plástico aplica las restricciones de diafragma en cada evento. Verificado con `examples/modelo_pruebaplastico.s3d` (pórtico 3D, 2 diafragmas): antes fallaba al instante; ahora **colapso plástico λc = 0.195 con 18 rótulas**. *(El pushover por control de desplazamiento sigue idealizando reticulado sólo-axial → para pórticos a flexión es «Rótulas plásticas», no es bug.)*
- ✅ **UX — menú «Análisis» reordenado** `[#20-a]`: los análisis se agrupan en dos **submenús flyout** — «Análisis lineales» (estático, modal, espectro) y «Análisis NL-lite» (no lineal, P-Delta, pandeo, form-finding, rótulas, pushover). Lo que **no es análisis** sale del menú: las **Combinaciones de carga** pasan a «Editar» (definición del modelo) y las **exportaciones de resultados + memorias PDF/Word** pasan a «Archivo» (centro de import/export). Quedan en «Análisis» sólo el estático directo (F5), recalcular, los diagnósticos (matrices K·M, estabilidad) y limpiar resultados.

## G12 · Análisis dinámico en el tiempo (time-history) 🟡 *(lineal cerrado; falta sólo el no-lineal #48b)*
*Respuesta dinámica ante un acelerograma en la base; se apoya en el modal (G2) y en las rótulas (G11).*
*Decisiones tomadas: integración por **Duhamel** por modo (es lineal, no hace falta Newmark); excitación **uniforme en la base** (sin soporte multi-apoyo); registros de ejemplo incluidos: **Llolleo y Constitución 2010**.*
- ✅ **Time-history lineal modal — MOTOR** `[#48a]`: `js/solver/timehistory.js` — superposición modal con **integral de Duhamel por modo** evaluada con la recurrencia **exacta de Nigam–Jennings** (Chopra Tabla 5.4.1; exacta para acelerograma lineal por tramos, incondicionalmente estable), **acelerograma uniforme en la base**. Para cada modo i: `q̈ᵢ + 2ζωᵢq̇ᵢ + ωᵢ²qᵢ = −Γᵢ·a_g(t)` con `Γᵢ = φᵢᵀM·r / φᵢᵀMφᵢ`; la respuesta física se reconstruye por `u(t) = Σ φᵢqᵢ(t)`. Núcleo autónomo (verificable en Node). Incluye `responseSpectrum` (auxiliar). **Verificado** (`test_timehistory.mjs`) contra soluciones analíticas SDOF (escalón → DLF=2; armónico → función de transferencia; vibración libre → decremento logarítmico) y un **Newmark directo independiente** en un edificio de corte 2-GDL (RMS modal−Newmark ≈ 8·10⁻⁶).
- ✅ **Entrada de acelerograma** `[#48c]`: `js/solver/accelerograms.js` — **parser** de un registro pegado/cargado (dos columnas `t a` o una columna `a` con Δt; salta cabeceras), **escala a PGA** y estadísticos (PGA, RMS, Arias). Señales de **demostración sintéticas** (claramente rotuladas como NO reales): pulso de Ricker, armónico y sismo sintético (ruido de banda con envolvente Saragoni–Hart, reproducible por *seed*). El diálogo permite **dirección X/Y/Z**, ζ, N° de modos, fuente (demo o pegar/cargar archivo) y escala. *(Los registros reales Llolleo/Constitución 2010 no se incluyen por no disponer de la serie digitalizada en el repo; se cargan/pegan como texto.)*
- ✅ **Resultados en el tiempo** `[#48d]`: integración modal en **Web Worker** (`timehistory_worker.js`; la superposición espacial la hace el hilo principal). Overlay «Respuesta en el tiempo»: **historia** del objeto monitoreado (gráfico SVG), **animación** de la deformada en el tiempo (slider + play, escala, botón «Máx» al instante de respuesta pico) y **exportación CSV**. `runTimeHistory` reúsa el modal por subespacio. Verificado en navegador (pórtico 3D con diafragmas): 10 modos, dir X/Y/Z, Γ por modo **coincide con la participación de masa** del modal (modo 2 domina con 87 %), u_máx 1.09·10⁻² m (X) / 1.12·10⁻² m (Y), animación y CSV OK.
  - **Monitor ELEGIBLE** (nodo o elemento): el overlay permite elegir **nodo** (u X/Y/Z, |u|, giros) o **elemento** (esfuerzos N, My, Mz por extremo, cortantes) y la curva/animación/CSV se recalculan para ese objeto. La historia de esfuerzos de elemento es `f_local(t) = Ke·(T·u_e(t))` por superposición modal.
  - **Hub de la barra**: el «Dinámico lineal (time-history)» aparece en la pestaña «Estático / lineal» del Centro de análisis (con «Ver» y casilla de lote); la **dirección X/Y/Z** se elige en su diálogo.
- 🟡 **Time-history NO LINEAL (rótulas plásticas)** `[#48b]` *(motor hecho y verificado; falta UI)*: `js/solver/nl_timehistory.js` — **integración directa Newmark-β** (γ=½, β=¼, incondicionalmente estable) con **Newton–Raphson por paso** y rigidez tangente. *(Decisión de diseño: una vez que el sistema fluye la base modal cambia paso a paso y la superposición modal/Duhamel deja de ser válida → el procedimiento riguroso es integración directa, como SAP2000/ETABS «Nonlinear Direct-Integration Time-History». El «modal por tramos» de la nota original sería equivalente sólo re-formando la base en cada evento; la integración directa lo subsume.)* Rótulas = resortes **bilineales con endurecimiento cinemático** (perfectamente plástico si α=0; descarga elástica e histéresis). Amortiguamiento de **Rayleigh** C=a₀M+a₁K₀. Núcleo autónomo con constructor de **edificio de corte** elastoplástico para validar. **Verificado** (`test_nl_timehistory.mjs`): límite elástico ≡ analítico (escalón DLF=2; armónico waveform RMS 0.04 %), elastoplástico SDOF y **MDOF 2 pisos** ≡ integración independiente por **diferencia central** (0.06–0.07 %), la fluencia reduce el pico y deja deriva residual, equilibrio dinámico 1.6·10⁻¹². **UI hecha** (`runNLTimeHistory`/`_nlthDialog`/`_nlthOpenOverlay`, menú Análisis → NL-lite + Centro de análisis con badge/«Ver»): reduce el modelo a un **edificio de corte** —pisos = diafragmas por z, masa del diafragma, **rigidez de entrepiso por análisis estático lateral ∝ masa**, Vy semilla = Cy·peso acumulado— en una **tabla editable** (el usuario afina la idealización), con dirección X/Y, ζ, endurecimiento α, acelerograma (demo/pegar/PGA) y T₁ en vivo. Overlay: historia del piso monitor + **diagrama «stick» animado** (pisos en fluencia en rojo) + slider/play + CSV. **Verificado en navegador**: pórtico 2 pisos con diafragmas → T₁ del edificio de corte **0.364 s vs T₁ modal real 0.39 s (7 %)**, ambos pisos fluyen bajo el sismo sintético. *(La idealización de corte es aproximada para pórticos a flexión —beam flexible, axial—; la tabla editable lo hace explícito. Pendiente fino: rótulas concentradas en extremos de barra del modelo completo en vez de entrepiso.)*
- ⬜ **Time-history modal con elementos de ÁREA** `[#51]`: confirmar/extender el time-history a modelos con membrana/placa/cáscara. La respuesta **nodal** (u(t)=Σφᵢqᵢ) ya debería funcionar porque el modal (`assembleK`) incluye los GDL de las áreas; falta (a) **verificar** un modelo con áreas (muro/losa) end-to-end, y (b) el **monitor de esfuerzo de elemento** sólo calcula extremos de **barra** (`f_local=Ke·T·uₑ`) — agregar la historia de **tensiones de área** (von Mises σ(t) por elemento) análoga al postproceso estático de áreas `[#49]`.
- ⬜ **Time-history con cáscaras NO LINEALES — DOS vías: completa (rigurosa) y simplificada (barras equivalentes)** `[#85]`: ofrecer **ambos caminos**, dejando claro el compromiso costo/fidelidad.
  - **(A) Completa / rigurosa** — integración directa **sobre la malla de cáscara real** (NL-th Newmark + Newton, reusando `[#48b]`), con la no linealidad de material/geometría en los propios elementos de área. **Más COSTOSA** (miles de GDL de cáscara → caro en el navegador, casos chicos o paciencia); es la referencia de fidelidad.
  - **(B) Simplificada** — **condensar cada panel/muro mallado a un macro-elemento de barra(s) equivalente(s)** —**corotacional** para grandes desplazamientos— resolver la dinámica sobre el **modelo reducido** (mucho menos GDL → integración directa viable y rápida) y **devolver/repartir los resultados integrados** (esfuerzos de panel, corte/momento, deriva) a las áreas para mantener la **coherencia** de visualización y postproceso `[#49]`. **Explícitamente SIMPLIFICADO**: rotular el método, sus hipótesis (rigidez de panel ↔ barra equivalente, comportamiento dominante) y sus límites en la UI/memoria.
  - *Estrategia: implementar primero la **simplificada (B)** (la que habilita el uso real en el navegador) y la **completa (A)** como verificación/opción de alta fidelidad cuando el tamaño lo permita. Encaja con `[#48b]` (NL-th directo) y `[#51]`.*

## G13 · Backlog — ideas nuevas detectadas en uso 🟡
*Mejoras de mayor alcance pedidas durante el uso.*
- 🟡 **Mesheador automático con motor tipo Gmsh** `[#52]`: **evaluado** (ver más abajo «Decisión de mallado #52») → en vez de embeber Gmsh (pesado, rompe offline/PWA) se construye un **mallador propio liviano** y autónomo (estilo `mesher.js`/`numeric.js`), reservando Gmsh como herramienta **offline** (CAD/mallas de verificación).
  - ✅ **Fase 1 — mallado TRANSFINITO (Coons) + multi-parche** (`js/model/mesh_map.js`): generaliza el mallador de bloque a regiones de 4 lados con **bordes curvos/poligonales** (trapecios, paralelogramos, sectores anulares) por interpolación de Coons; con lados rectos coincide exactamente con el bilineal. **QUAD-dominante** (QUAD4+MITC4, mejor que CST). **Submapping** manual por parches con **soldadura** de nodos (plantas en L/U conformes). Chequeo de **Jacobiano** (avisa elementos invertidos). Integración con el modelo (`meshRegionIntoModel`/`meshPatchesIntoModel`) soldando a nodos existentes; `mallarPanelSeleccion` ya usa el motor transfinito. Verificado: `test_mesh_map.mjs` (Coons≡bilineal, sigue arco R=4→6, Jacobiano, soldadura) + **patch test** de membrana en malla trapezoidal distorsionada (verif **3-001**, σ₁=E·εx a precisión de máquina vía desplazamiento prescrito #54).
  - ✅ **Fase 2 — calidad de malla** (`js/model/mesh_quality.js`): métricas por elemento (triángulo: ángulos + forma normalizada; QUAD: Jacobiano escalado, ángulos, aspecto, **alabeo**), estadística global con peores celdas + histograma (`meshStats`), y **suavizado Laplaciano RESTRINGIDO** (`laplacianSmooth`/`smoothAreasInModel`) que mueve los nodos interiores sin invertir elementos (fija bordes/apoyos/cargas). Verificado: `test_mesh_free.mjs` (equilátero→calidad 1, cuadrado→Jac 1, suavizado sube la calidad mínima sin mover el borde).
  - ✅ **Fase 3 — malla LIBRE triángulo/quad** (`js/model/mesh_free.js`): **ear-clipping** de polígono simple (cóncavo OK: plantas en L/U) → **flips de Delaunay** → **refinamiento** uniforme al tamaño h → **recombinación a cuadriláteros** (voraz por calidad) → suavizado. `meshPolygonIntoModel` proyecta el polígono 3D a su plano y suelda a los nodos existentes. Verificado: `test_mesh_free.mjs` (conservación de área cuadrado=4/L=3, sin invertidos, Delaunay no empeora ángulos) + **patch test** en malla libre de una L (verif **3-005**, σ₁=E·εx exacto en malla QUAD/triángulo mixta). ✅ **Agujeros** (huecos): `triangulatePolygon`/`meshPolygonIntoModel` aceptan `holes:[[[x,y]…]…]` y los fusionan al contorno por **puentes (bridging estilo earcut)** → malla conforme con huecos. Verificado: cuadrado con hueco (Σárea=16−1=15, sin invertidos) + **patch test con agujero** (σ₁=E·εx exacto). ✅ **Triángulo membrana con DOF de giro (Allman 1984)**: `allmanElement`/`allmanStress` en `membrane.js` (LST de 6 nodos con GDL de medio-lado sustituidos por rotaciones de esquina), estabilización por resorte diagonal mínimo del drilling (εd=1e-3, no Hughes-Brezzi que rigidizaba la flexión), flag `area.drilling` (round-trip) + UI (checkbox individual/masivo). Supera el bloqueo por corte del CST (verif **3-006**: 8×2 recupera ~57 % vs CST ~26 %); pasa patch test y 3 modos de cuerpo rígido (`test_allman.mjs`).
  - ✅ **Verificación contra TESIS publicada — network arch** (`tools/build_network_arch_verif.mjs`): reproduce el puente ferroviario de doble vía de **Brunn & Schanack** (TU Dresden: 100 m luz, 17 m flecha, 44 péndolas inclinadas cruzadas, arco W360×410×990, g_k=125 kN/m) y compara la **1ª frecuencia de flexión**: Pórtico **2.53 Hz** vs tesis **2.34 Hz** → **8 %** (ambas dentro de la ventana EN1991-3 1.54–3.02 Hz). Doc en `docs/verificaciones/`.
  - ✅ **UI del mallador y links** (menú Editar): **«Mallar región libre»** (selecciona N nodos del contorno → malla libre quad/tri vía `meshPolygonIntoModel`, soldando al contorno), **«Suavizar malla (calidad)»** (`smoothAreasInModel`, reporta calidad mín. antes/después) y **«Crear link / coupling»** (2 nodos → link rígido con brazo o coupling de GDL elegidos). El panel de 4 esquinas sigue para el mallado transfinito. ✅ **Render del link en el viewport**: `viewport.refreshLinks`/`_buildLinkViz` dibuja línea maestro→esclavo (sólida **negra**=rígido / punteada púrpura=coupling), marcador en el maestro y cono de dirección.
- ⬜ **Macromodelos (p.ej. muros de relleno de albañilería / infill walls)** `[#86]`: incorporar **macromodelos** que resuelven la no linealidad de subsistemas complejos con **pocos elementos calibrados** en vez de un mallado fino: combinaciones de **barras + cables + links/resortes NO lineales** (p.ej. el **puntal diagonal equivalente** del muro de relleno de albañilería —single/double/triple strut—, con su backbone de degradación), pero **presentadas y editadas como un objeto 2D** (un «panel») que el usuario inserta como **una sola entidad** y el motor **expande** a su red interna de elementos (reusando las rótulas/resortes NL de `nl_lite.js` y los links de `links.js`). **Catálogo de macromodelos** (infill de albañilería, conexiones semirrígidas, aisladores/disipadores…). *(Resuelve la no linealidad del relleno sin malla de cáscara; encaja con NL-lite de cables/resortes, con los links `[#G14]` y con la reducción a barras de `[#85]`.)*
- ⬜ **La IA entiende una torre de alta tensión** `[#53]`: extender el asistente (`asistente/generador.js` + Worker) para describir/generar **torres de transmisión** (celosías 3D): patas, crucetas, ménsulas, diagonales/redundantes, conexiones articuladas (releases), cargas de viento/cable/hielo y combinaciones. Requiere vocabulario de *ficha* para celosías espaciales y un generador paramétrico de torre. *(Caso de uso real del IOC; encaja con NL-lite de cables para los conductores.)*

## G14 · Implementar capacidades parciales / ausentes (detectadas en verificación) 🟡 *(5 hechas; resto menor)*
*Cierre de las brechas mapeadas en [`docs/capacidades-portico.md`](capacidades-portico.md). Cada una habilita uno o más casos de verificación CSI.*
- ✅ **Desplazamiento prescrito de nodo / apoyo** `[#54]`: GDL con desplazamiento/giro impuesto como **propiedad del nodo** (`node.prescDisp = {ux..rz}`, en m/rad), editable en el panel del nodo (sección «Desplazamiento prescrito»). El solver lo trata como GDL soporte con valor: `Kff·uf = Ff − Kfp·u_p`, superpone `u_p` y recalcula reacciones con `K·u−F` (reacción del apoyo asentado correcta). Funciona en el `StaticSolver` denso y en el **camino del worker** (`_solveStaticCases` fuerza K densa + traslación al RHS cuando hay prescritos). Verificado: voladizo con asentamiento de punta → V=3EIδ/L³, M=3EIδ/L² (`test_prescdisp.mjs`, exacto; y en navegador). Habilita **1-005** (settlement) y los patch tests con desplazamiento prescrito. *(Decisión D2.)*
- ✅ **Modal con rigidez geométrica (Kg)** `[#55]`: toggle **«Incluir rigidez geométrica P-Δ (pre-esfuerzo)»** + selector de estado de referencia (caso/combo) en el diálogo modal (como SAP2000). `runModal` resuelve el estático de referencia, ensambla `Kg` (`assembleKg`) y corre el modal sobre `K+Kg` → tracción rigidiza / compresión ablanda las frecuencias. Verificado autoconsistente con el FE: `ω²(P)/ω²(0) = 1 − P/Pcr` con `Pcr=λcr·P` (`test_modal_kg.mjs`; `Pcr` ≡ Euler π²EI/L²). Habilita **1-017** (cuerda tensa) y modal de cables/pretensados. *(Decisión D3.)*
- ✅ **Miembros «compression-only»** `[#56]`: flag por elemento `el.compressionOnly` (puntal/contacto, N=0 en tracción), **excluyente con `cable`** (en model y UI), check «Puntal (solo compresión)» en el panel del elemento. Espejo del cable tension-only en `barState` (`nl_lite.js`). Round-trip `.s3d`, copia de elemento y deformada NL (suelto = punteado gris). Verificado (`test_componly.mjs`): tracción→N=0/suelto, compresión→N=−Fx/activo. Habilita **1-012** (no-tension/no-compression frame). *(Decisión D1.)*
- ✅ **Gradiente térmico a través del espesor en áreas** `[#57]`: temperatura **por cara** (🟥 cara +z, 🟦 cara −z, como Abaqus) en el panel del área + caso de carga. Media → dilatación de membrana; diferencia (T_sup−T_inf) → **curvatura térmica** κ₀=α·ΔT/t → momento de flexión (`plateThermalLoad` MITC4/DKT en `plate.js`, ensamblado a global con el transform de placa; `areaBendingStress` descuenta el momento térmico M_T=Db·κ₀). Verificado patch test (`test_thermal_gradient.mjs`): placa libre adopta κ=κ₀ con momento mecánico ≈0; σ restringido = E·α·ΔT/(2(1−ν)). Habilita **2-014**. *(Decisión D4.)*
- ✅ **Continuo plano: tensión plana y deformación plana** `[#58]`: flag **por área** `area.planeStrain` (check «Deformación plana (membrana)» en el panel), `Dmatrix` con la constitutiva plane-stress/plane-strain y la deformación térmica amplificada por (1+ν) en deformación plana. Verificado (`test_plane_strain.mjs`): membrana uniaxial → plane-stress εx=σx/E, plane-strain εx=σx(1−ν²)/E y εy=−ν(1+ν)σx/E (más rígida). Habilita **3-002/3-003** (plane-stress) y **3-004** (plane-strain). *(Decisión D5.)*
- ⬜ **Otras parciales** (en `capacidades-portico.md`): fijación parcial de extremo (resorte de extremo, **1-008**), resorte de **línea**/viga sobre fundación (**1-013**), **pandeo de cáscara** (**2-016/2-017**), **gran rotación con flexión** (viga corotacional, **1-029**). Prioridad menor.

### Para análisis de PUENTES *(motores implementados y verificados 🟡 — falta UI)*
- ✅ **Etapas constructivas** (staged construction) `[#59]`: `js/solver/staged.js` (`StagedSolver`) — análisis lineal **incremental por etapas** con activación/desactivación de **elementos** y de **apoyos** (puntales/cimbra) por fase, y **acumulación de estado** (U y esfuerzos por elemento sólo desde su activación → cada dovela «nace» libre de tensión, como CSiBridge). Peso propio de los elementos recién activados aplicado al colarse. Verificado: voladizo apuntalado por fases (`test_staged.mjs` + verif **1-031**, 0.0 %) — flecha/momentos **distintos** del montaje monolítico, cada uno ≡ solución analítica de viga. *(Limitación: sólo barras; áreas/diafragmas se ignoran en el ensamble por etapas.)*
- ✅ **Pretensado por tendones** `[#60]`: `js/solver/tendon.js` — tendón con **trazado** parabólico o **poligonal**, fuerza efectiva o de tesado con **pérdidas** por fricción/ondulación (`P(x)=P₀e^{−(μθ+kx)}`) + tanto alzado, traducido a **cargas equivalentes** (UDL de balanceo `w=8Pa/L²` + axial y momentos de ancla). Verificado: viga PT (`test_tendon.mjs` + verif **1-009**, ≤0.011 %) — contraflecha `5wL⁴/384EI`, axial −P, **balanceo de carga** (flecha neta ≈0) y momento primario P·e. *(Encaja con `#57`/`#55`.)*
- ✅ **Cargas móviles / líneas de influencia** `[#61]`: `js/solver/moving_load.js` — **pista** (lane) de elementos, **barrido** de una carga unitaria o un **tren** multi-eje (K factorizada una vez), **líneas de influencia** y **envolventes** de esfuerzos/reacciones; reparto de la carga puntual por funciones de forma consistentes (Hermite) → respuesta nodal exacta. Verificado: viga simple (`test_moving.mjs` + verif **1-030**, 0.0 %) — LI reacción ≡ 1−x/L, LI momento de centro triángulo de pico L/4, envolvente de tren de 2 ejes. *(Base de CSiBridge para tránsito.)*
- ✅ **Librería de ejemplos de puentes** (`tools/build_bridges.mjs` → `examples/puente_*.s3d` + `docs/ejemplos/*.pdf`): modelos paramétricos 2D que resuelven en equilibrio, con figura deformada y ficha PDF (membrete IOC): **atirantado** (pilón central + tirantes en abanico), **colgante** (cable parabólico + péndolas + torres + anclajes), **arco atirantado/bowstring** (tablero-tirante, apoyos sólo verticales) y **arco de tablero inferior tipo Barqueta** (through arch, péndolas + tirantes de pie). *(Pendiente: arco atirantado tipo **network** como verificación contra una memoria de tesis que aportará el usuario.)*
- ✅ **Puentes con geometría REAL** (`tools/build_bridges_real.mjs` + `tools/build_barqueta.mjs`, fuentes web citadas): **Salginatobel** (arco H.A. 3 rótulas, 90 m), **Golden Gate** (colgante 1280 m, cable parabólico colgante), **Severin/Severinsbrücke** (atirantado pilón-A, 302 m), **Treng Treng-Kay Kay** (Temuco, río Cautín: atirantado asimétrico de **mástil único**, 240 m / vano 140 m) y **Puente de la Barqueta** (3D: arco central + **péndolas casi verticales en plano único, sin cruzarse** + **tablero con elementos de área shell** + **análisis geométrico no lineal P-Δ**). `.s3d` + `.pdf` en `docs/ejemplos/`. *(Cables suspensión/atirantado en lineal con I surrogate; cable real → Kg/NL-lite.)*
- ✅ **Caso de estudio — arco: lineal + pandeo lateral + etapas** (`tools/build_arch_showcase.mjs`): un mismo arco atirantado 3D (100 m) analizado en 3 niveles: **estático lineal**, **pandeo LATERAL del arco** (autovalores `(K+λKg)φ=0`, `solveBuckling` → λcr≈35, modo fuera del plano confirmado) y **etapas constructivas** (arco → tablero+péndolas → sobrecarga). 3 figuras (deformada / modo de pandeo / etapas) + PDF. Combina los motores estático + pandeo + staged.
- ✅ **Tutorial 2 — edificio de MUROS en Valdivia** (`tools/build_valdivia_muros.mjs`): 5 pisos con **muros = elementos membrana** + **losas = elementos placa** + diafragmas; modal real (T₁≈0.09 s, mucho más rígido que el de pórticos) + espectro NCh433/DS61.
- ✅ **Tutorial — edificio 5 pisos H.A. en Valdivia** (`tools/build_tutorial_valdivia.mjs` → `examples/tutorial_edificio_valdivia.s3d` + `docs/ejemplos/tutorial_edificio_valdivia.pdf`): tutorial paso a paso end-to-end (geometría 3×3 vanos rectangular / 5 pisos, materiales H30, cargas NCh1537, masas sísmicas, diafragmas, **modal** real corrido headless → T₁=0.65 s con 81 % de masa en el modo fundamental, espectro **NCh433/DS61** Zona 2 / Suelo D, cortes basales estático/modal/mínimo, derivas, combinaciones NCh3171). Con caveats del sitio (Valdivia: suelos blandos, confirmar zona/suelo).
- ✅ **UI de los tres motores de puente**: submenú **Análisis → Puentes** + filas en el Centro de análisis (pestaña «Avanzados», sección Puentes, con badge ✓/«Ver»). (a) **Etapas constructivas** (`runStaged`/`_stagedDialog`): editor de secuencia que activa **grupos** de elementos en orden (▲▼/🗑, peso propio nuevo por etapa) + caso de **servicio** opcional en la última etapa; deformada acumulada vía `showNLDeformed`. (b) **Pretensado por tendón** (`runTendon`/`_tendonDialog`): trazado parábola/poligonal, excentricidades e↓+, fuerza efectiva P **o** tesado+pérdidas (μ/k/tanto alzado), aplica cargas equivalentes a un caso y opcionalmente resuelve. (c) **Cargas móviles** (`runMovingLoad`/`_movingDialog`): pista = elementos seleccionados, **línea de influencia** (carga unitaria) o **envolvente** (tren multi-eje), respuesta = reacción de apoyo o esfuerzo en sección (ξ); overlay flotante `#ml-overlay` con gráfico SVG (máx/mín marcados) + CSV. Resultados reutilizables (`_stagedResult`/`_movingResult`, limpiados en `clearResults`/`_loadJSON`). Verificado en navegador: etapas (Mz=w·L²/2 con peso propio), tendón (w_eq=8Pa/L²=24 kN/m), IL de reacción ≡1 en apoyo único y IL de momento variable.
- ✅ **Links / couplings** (restricciones cinemáticas): `js/solver/links.js` — liga un nodo **esclavo** a un **maestro** sin elemento entre ellos. **Link rígido** (con brazo/offset): `u_s = u_m + θ_m×r`, `θ_s=θ_m` → transmite fuerza+momento (tablero modelado arriba acoplado a vigas/pilas; *end offsets* 1-010, *insertion points* 1-011, apoyos excéntricos). **Coupling simple**: iguala los GDL elegidos. Penalización (como diafragmas), en el camino denso y disperso; round-trip `.s3d`. Verificado: `test_links.mjs` + verif **1-010** (tablero excéntrico → momento base P·e, **0.0 %**). *(Falta UI: crear link seleccionando 2 nodos.)*
- ⬜ **Cacho rígido NATIVO (zona rígida de extremo / end length offset)** `[#87]`: propiedad **por elemento** (`el.rigidEnd = {i, j}` en longitud (m) o factor 0–1 del nudo) que **condensa un brazo rígido en cada extremo DENTRO de la matriz del elemento** (transformación `T_rigid` aplicada a `K`/`M` y a las fuerzas de empotramiento) y **acorta la luz flexible** automáticamente — el «cacho rígido viga-columna» clásico de SAP2000/ETABS para nudos de pórtico, **sin** crear nodos/links extra. Hoy esto sólo se puede emular con un **link rígido** (nodo+link, ✅ arriba), que es un constraint cinemático, no la condensación en el elemento. Incluye **UI** en el panel del elemento (longitud/factor por extremo) + **test** contra solución analítica (viga con tramos rígidos en extremos → menor flecha/giros que la luz total). *(Encaja con `[#54]` y con la fijación parcial de extremo 1-008.)*
- ⬜ **Apoyos con desplazamiento prescrito** → cubierto por `[#54]` (asentamientos diferenciales de pilas/estribos, típicos en puentes).
- 🔗 **Ya en el backlog**: time-history modal con **elementos de área** `[#51]` (en G12) · **mesheador automático tipo Gmsh** `[#52]` (en G13) — ambos también cierran brechas de capacidad.

## G15 · Diseño multinorma generalizado + API pública ✅ `[#62]`
*Diseño de elementos rediseñado de raíz y fachada pública para consumir Pórtico.*
- ✅ **Diseño GENERALIZADO (resistencias en el material, forma en la sección)**: antes los parámetros vivían en un JSON global por tipología (un único Fy para todo el acero) y el tipo se adivinaba por el **nombre** → un material con propiedades distintas no se podía diseñar. Ahora las resistencias son propiedades del **material** (`mat.design.{family,Fy,Fu,fc,fyRebar,Fb,…}`, en MPa; `js/design/material_props.js`) y la geometría de diseño viene de la **forma** de la sección (`sec.design.{shape,dims}`; `js/design/section_props.js` calcula S, módulos **plásticos Z**, r, Cw, Av y esbelteces para I/rect/círculo/tubo/caja, rectángulo equivalente para genérica). Verificado vs **IPE300 tabulado** (≤6 %). Fallback al JSON legado (`diseno_params.json`) por compatibilidad.
- ✅ **Códigos de diseño CONECTABLES** (`js/design/registry.js`): **AISC 360-16 LRFD y ASD** (D2, E3, **F2 con pandeo lateral-torsional** Lp/Lr/Cb, F6, G2, H1.1; `codes/aisc360.js`), **Eurocódigo 3 EN1993-1-1** (clasificación 5.5, curvas de pandeo χ, LTB χLT con Mcr, corte, interacción; `codes/eurocode3.js`), **ACI 318-19 / EC2** hormigón (`codes/concrete.js`) y **NCh1198** madera (`codes/timber.js`), todos leyendo del material. `diseno.js` es el ORQUESTADOR (`verificarElemento` elige el código por `model.designSettings.codeByFamily` o el default de la familia; firma compatible). Verificado vs cálculos de manual (`test_design.mjs`) y caso **4-001** (φRn AISC vs fórmulas con props tabuladas, ≤6 %, 3 tramos de LTB).
- ✅ **API PÚBLICA** (`js/api/portico.js`): clase `Portico` (Node + navegador) — **pre** (build/import/export/mesh), **solver** (estático, modal, modal+Kg, pandeo, etapas), **post** (desplazamientos, reacciones, esfuerzos, diagramas, **diseño multinorma**) y **extensibilidad** (`Portico.registerAnalysis`, `registerDesignCode`). Verificado end-to-end (`test_api.mjs`). Docs: [`docs/diseno.md`](diseno.md), [`docs/api.md`](api.md).
- ✅ **UI de diseño**: bloque «Diseño» en la tarjeta de material (familia + resistencias), «Forma (diseño)» en la sección (forma + dimensiones + armadura + botón «Calcular A, I, J desde la forma»), y **selector de código por familia** en el panel de Diseño (como SAP2000, fija `designSettings` y re-verifica).

## G16 · Pendientes normativos ⬜
*Brechas de fidelidad y cobertura del motor de diseño (G15), agrupadas. Ninguna bloquea el uso actual; suben la fidelidad a SAP2000 y amplían las normas.*
- ✅ **Aluminio con código propio** `[#63]`: `js/design/codes/eurocode9.js` — **EN 1999-1-1 (Eurocódigo 9)** registrado y fijado como default de la familia `aluminum` (antes usaba AISC). Cubre tracción (No,Rd con sección bruta/neta), compresión `Nb,Rd=κ·χ·A·fo/γM1` con las **curvas de pandeo EC9** (clase A α=0.20/λ̄0=0.10, clase B α=0.32) y **factor HAZ κ** (reducción por soldadura), flexión+LTB (`α·Wel·fo/γM1`), corte e interacción. Usa `fo` (límite 0.2 %) y E del material (≈70 GPa), γM1=1.10/γM2=1.25. Aparece solo en el selector de código por familia. Verificado (`test_aluminum.mjs`): No,Rd, χ de la curva A, Nb,Rd, Mc,Rd, Vo,Rd, κ HAZ, ruteo del orquestador y χ_aluminio<χ_acero por menor E. *(Pendiente fino: interacción 6.3.3 con exponentes ξ/η/γ; AA ADM como alternativa.)*
- ✅ **Interacción rigurosa de Eurocódigo 3** `[#64]`: implementadas las ecs. **6.61/6.62** de EN 1993-1-1 6.3.3 con los factores **kyy/kzz/kyz/kzy** del **Anexo B (Método 2)** (clase 1/2, miembros susceptibles a torsión) en flexo-compresión; χy/χz por eje, χLT, Cmy/Cmz/CmLT configurables. Mapeo de ejes documentado (código z=mayor, y=menor). Tracción+flexión sigue lineal (6.2.1). Verificado (`test_ec3_interaction.mjs`): compresión pura ≡ ratio axial (exacto), kyz=0.6·kzz, kij en rango, monotonía con el momento, Cmy menor ⇒ menor ratio.
- 🟡 **Diagrama de interacción P-M de hormigón** `[#65]` *(uniaxial hecho)*: `pmDiagram`/`pmRatio` en `concrete.js` construyen el **diagrama P-M real** por compatibilidad de deformaciones (εcu=0.003) + **bloque de Whitney** (β1 por f'c), acero elastoplástico ±fy, armado simétrico en 2 capas, **φ variable** (0.65→0.90 por εt) y tope ACI 22.4.2 (0.80·φ·Po); el D/C se evalúa **radialmente** (rayo origen→demanda vs envolvente). Reemplaza la interacción lineal; eje de flexión = el del momento dominante. Verificado (`test_concrete_pm.mjs`): compresión pura en el tope → D/C≈1, escala radial lineal con M, nariz del balanceado φMn>φMn de flexión pura. ✅ **Biaxial P-Mz-My** por **método del contorno de carga**: `(Mz/Mnz)^α+(My/Mny)^α≤1` con Mnz,Mny = capacidad uniaxial al axial Pu en cada eje (`pmMomentAt`), α configurable (def. 1, conservador). Verificado (`test_concrete_pm.mjs`): columna simétrica 120+120 ≈ 2× la uniaxial; α mayor → menos conservador. *(Pendiente: armado multicapa explícito en biaxial, parábola-rectángulo EC2.)*
- ✅ **Catálogo de perfiles tabulados** `[#66]`: `js/design/profiles.js` — biblioteca de perfiles comerciales (**IPE 80–600**, **HEA 100–400**, **HEB 100–400**, **CHS** tubos, **RHS/SHS** cajas) con sus dimensiones nominales; `profileToSection(name)` deriva A/Iz/Iy/J + forma+dims (vía `fromShape`) para asignar de una vez al modelo (solver + diseño coherentes). UI: selector «Perfil tabulado (catálogo)» con optgroups por familia en el panel de sección (panel «Forma (diseño)»); al elegir, escribe la sección y re-renderiza. Verificado (`test_profiles.mjs` + navegador): IPE300/HEB200 A·Iz·Zz ≈ tabulado (nominal ~3–4 % por debajo por los radios de acuerdo, lado conservador), resolución de familias y aplicación al modelo. *(Pendiente: valores EXACTOS tabulados con redondeos alma-ala/Cw/J en vez de geometría nominal; W/HSS AISC e IPN.)*
- 🟡 **Más formas de sección para el diseño** `[#67]` *(formas C/L/T hechas; checks especializados pendientes)*: añadidas **canal (C/U)**, **ángulo (L)** y **tee (T)** a `section_props.js` vía un helper de **compuesto de rectángulos** (`rectsProps`) que calcula A, centroide, Iz/Iy, módulos elásticos S y **módulos plásticos Z** con el eje neutro plástico por bisección + integral analítica del momento. J de perfil abierto delgado; **Cw de canal** por fórmula estándar (alabeo de L/T despreciable). UI: las tres formas en el selector «Forma (diseño)» + sus dimensiones. **Verificado** (`test_section_shapes.mjs`): A/I/S/Z de C(UPN200)/L(100×75×10)/T(150×100) ≡ integración numérica por celda fina (<0.1 %); diseño AISC fluye (canal: Mn=73.2 kN·m, interacción gobierna). ✅ **Checks de miembro F9/F10** (`aisc360.js`): **tee → AISC F9** (fluencia min(Fy·Zz,1.6·Fy·Sz) con alma traccionada/comprimida + LTB Mcr=(1.95E/Lb)√(IyJ)(B+√(1+B²))) y **angular → AISC F10** (fluencia 1.5·My + LTB con Me=0.46·E·b⁴·t·Cb/Lb²). Verificado (`test_f9f10.mjs`): tee Mn 43.5→23.8 kN·m al crecer Lb; angular muy débil a LTB (11.7→1.2). *(Pendiente: centro de cortante del canal; secciones asimétricas arbitrarias.)*
- 🟡 **Servicio y detallado por norma** `[#68]` *(servicio hecho; detallado sísmico pendiente)*: `js/design/serviceability.js` — límites de **flecha** (L/divisor por código y uso: IBC/AISC L/360·live, L/240·total; EC L/250; NCh; voladizo → 2L) y de **deriva** Δ/h por código (NCh433 0.002, ASCE7/IBC 0.020, EC8 0.010), con `checkDeflection`/`checkDrift`. Integrado: el **time-history no lineal** muestra en su overlay la deriva máx Δ/h vs el límite NCh433 (ratio en verde/rojo); expuesto en la **API** (`Portico.checkDeflection`/`checkDrift`). Verificado (`test_serviceability.mjs`): L/360, voladizo 2L, EC vs AISC, NCh433 vs ASCE7, overrides. ✅ **Detallado sísmico columna fuerte–viga débil** (`js/design/seismic.js`): `strongColumnWeakBeam({sumMnc,sumMnb,gamma})` (ACI 18.7.3 γ=1.2 / AISC 341) + `jointSCWB(model, MnOf)` que recorre los nudos, clasifica cada barra como columna/viga por **verticalidad** y compara ΣMnc ≥ γ·ΣMnb; expuesto en la API (`Portico.seismicSCWB`, con Mn≈Fy·Zz por defecto). Verificado (`test_seismic.mjs`): chequeo puntual, clasificación, nudo cruz 2col+2vigas (ΣMnc=360 ≥ 1.2·240). *(Pendiente: flecha relativa a la cuerda por elemento en la tabla de diseño —hoy sólo |δ| nodal—; UI de nudos SCWB en el viewport.)*

## G17 · Módulo de secciones, catálogos y diseño automático 🟡 *(función avanzada — token PRO)*
*Section designer ampliado + biblioteca precargada + predimensionar + diseñar (auto-selección desde catálogo). **Regla central (no negociable): el sistema NUNCA inventa materiales, perfiles, escuadrías ni barras; sólo elige desde los catálogos ya cargados en el modelo.** Si ningún candidato cumple, lo informa claramente y sugiere agregar más candidatos. Apoya en G15 (motor de diseño multinorma) y reusa/absorbe G16 `[#66]`/`[#67]`. Mantener carácter docente + advertencia «diseño preliminar, requiere revisión profesional».*

- ✅ **Biblioteca precargada de materiales y perfiles** `[#69]`: **Materiales** — `js/design/materials_catalog.js` (acero A36/A572-50/S275/S355, hormigón H25/H30/H40, madera C16/C24, aluminio 6061-T6), cada uno con E/G/ν/ρ/α en unidades del modelo + bloque `design` (familia + resistencias MPa); UI: selector «Insertar material estándar» (optgroups por familia) en el panel de Materiales que inserta y queda editable. Verificado (`test_matcat.mjs` + navegador: resuelven a la familia/resistencias correctas, copia profunda). **Perfiles** — `profiles.js` (`[#66]`, IPE/HEA/HEB/CHS/RHS). *(Pendiente: más escuadrías de H.A./madera tabuladas, perfiles galvanizados, barras de refuerzo como catálogo.)*
- 🟡 **Editor de secciones tipo «Section Designer» de SAP2000** `[#70]` *(polígono/huecos hechos; falta H.A. con barras y compuestas)*: ✅ rect, circular, tubular, **I/H** + **C/L/T** (`[#67]`); ✅ **polígono libre y con huecos** — `js/design/polygon_props.js` (teorema de Green) calcula A, **centroide (y,z)**, Iz, Iy, **producto Iyz**, **ejes principales (I₁,I₂,θ)**, Sz/Sy, **Zz/Zy plástico** (recorte de semiplano), perímetro y envolvente; soporta cóncavas y con huecos. Integrado como forma `'polygon'` en `section_props.js` (J≈A⁴/40Ip aprox., Av≈⅚A) con Iyz/principales propagados; **UI**: forma «Polígono libre / con huecos» en el panel de sección con textareas de vértices «x,y» + huecos, y el botón «Calcular A,I,J». **peso por metro** según material ya en el auto-diseño (`weightPerM`). Verificado (`test_polygon_props.mjs`, 20/20 a precisión de máquina: rect, cajón con hueco, L —Iyz≠0, simetría diagonal—, sección girada 30° —principales recuperan los I sin girar—) + navegador. ✅ **Hormigón con barras longitudinales + estribos**: `rebarLayers` en `concrete.js` arma las capas del **diagrama P-M** desde barras explícitas (`rebar.{nTop,nBot,dia_mm}` o `layers:[{n,dia,d}]`) en vez de sólo ρ; los **estribos** (`rebar.{estribo_dia_mm,estribo_s_mm}`) suman `φVs=φ·Av·fy·d/s` al corte (tope ACI). `resolveSectionProps` propaga `rebar`; UI: campos n sup./n inf./φ barra y estribo φ/sep. en el panel de sección. Verificado (`test_concrete_pm.mjs`: barras ≈ ρ equivalente 0.19 %, estribos 101→219 kN, y por la ruta real `verificarElemento`). ✅ **Secciones compuestas** (acero-madera, acero-hormigón): `compositeProps({parts,Ebase})` en `polygon_props.js` por el **método de la sección transformada** (razón modular n=E/Ebase) → área e inercias transformadas, eje neutro y EI=Ebase·Iz_tr; expuesto en la API (`Portico.compositeProps`). Verificado (`test_polygon_props.mjs`): misma E ≡ monolítica; madera+plancha de acero ≡ cálculo a mano a precisión de máquina (eje neutro sube hacia el acero, EI correcto). *(Pendiente: editor gráfico de vértices.)*
- ✅ **Predimensionar** `[#71]`: `predimensionar(...)` en `js/design/autodesign.js` — reglas simples ANTES del análisis: viga acero (canto≈L/20 → IPE del catálogo), columna acero (lado≈H/15 → HEB), viga H.A. (h≈L/11, b≈h/2, constructivas), columna H.A. (Ag≈N/(0.35·f'c)), madera (h≈L/17, b≈h/3). **UI** (`predimensionarDialog`, botón «⚡ Predimensionar» en la pestaña Diseño): tipo/material/L/carga → propuesta en vivo + opción «crear sección y asignar a la selección». Verificado (`test_autodesign.mjs` + navegador: viga H.A. L=7 → 65×35 cm).
- ✅ **Diseñar (auto-selección desde catálogo)** `[#72]`: `seleccionarPerfil({demands, candidates, mat, code, member, prefs})` en `autodesign.js` — recorre los candidatos (catálogo `profiles.js` o lista explícita), verifica cada uno con el **motor G15** (`verificarElemento`) y elige el mejor por **puntaje** (no la primera que cumple): D/C≤1, banda objetivo 0.75–0.90, minimizar peso, **preferir secciones repetidas** (continuidad). **NUNCA inventa**: si ninguno cumple, `best=null` + nota. `steelCandidates(['IPE','HEA','HEB'])`. Verificado (`test_autodesign.mjs` + navegador): viga Mz=120 → IPE270; viga sub-dimensionada D/C=6.99 → propone **IPE360** D/C=0.85. ✅ **Multinorma (acero/H.A./madera)**: `candidatesForFamily(fam)` → catálogo de acero (IPE/HEA/HEB), `concreteCandidates` (escuadrías cuadradas/rect 20–80 cm con ρ) o `timberCandidates` (escuadrías b×h); `autoDesignDialog` agrupa por sección, detecta la familia del material y propone candidatos de esa familia; `_autoDesignApply` aplica la **sección candidata** (no sólo perfiles del catálogo). Verificado en navegador: viga H.A. sub-dimensionada D/C=5.41 → **H.A. 40×60** (D/C 0.77), aplica y renombra.
- 🟡 **UX de la pestaña Diseño + preview no destructivo** `[#73]` *(núcleo hecho)*: botones **«⚡ Predimensionar»** y **«🧮 Diseñar (auto)»** junto a «Verificar diseño». El auto-diseño usa el **alcance** = selección o todo el modelo, agrupa por **sección** (continuidad), y abre un **overlay de VISTA PREVIA no destructiva** (`#ad-overlay`): por sección → actual · propuesta · D/C (color) · kg/m · modo gobernante · aviso «sección compartida», con casilla por fila; nada cambia hasta **«✓ Aplicar al modelo»** (con **snapshot/undo**, renombra la sección al perfil). Verificado en navegador (D/C 6.99 → IPE360, aplica y crece la sección, Ctrl+Z deshace). *(Pendiente: selector de material permitido explícito, panel de preferencias avanzado, reporte de diseño, H.A./madera en el auto-diseño.)*

## G18 · Interoperabilidad SAP2000 / ETABS (texto) ⬜
*Importar/exportar modelos por archivos de texto, además del `.s3d`/CSV propios.*
- ⬜ **Módulo de intercambio con modelo neutro** `[#74]`: arquitectura limpia, extensible y testeable `PorticoModel ↔ NeutralStructuralModel ↔ {SAP2000, ETABS}`. **Exportar**: `exportToSAP2000S2K()` (`.s2k`/`.$2k`), `exportToETABSE2K()` (`.e2k`). **Importar**: `importFromSAP2000S2K()`, `importFromETABSE2K()`. Mapear nodos/joints, frames, restraints, materiales, secciones, casos/combos y cargas; advertir lo no soportado en cada sentido. Conserva el formato propio (JSON/CSV). 100 % navegador, sin servidor; verificable headless con archivos de muestra.

## G19 · Importador IFC (BIM-agnostic) ⬜
*Leer modelos BIM estructurales, previsualizar, elegir qué importar y comparar side-by-side antes de confirmar. Librería web (preferente **web-ifc** / That Open Components `IfcLoader`); todo en el navegador, sin subir a servidor.*
- ⬜ **Carga y clasificación IFC** `[#75]`: `ifcLoader.js` (parsea geometría/datos del `.ifc` local) + `ifcClassifier.js` (clasifica IfcBeam/IfcColumn/IfcMember/IfcWall/IfcSlab/IfcPlate). **Sólo se importa lo soportado por PÓRTICO**: hoy barras → IfcBeam/IfcColumn/IfcMember (muros/losas se listan como «no soportado todavía»; activarlos cuando haya import real de áreas — IfcWall→muro, IfcSlab→losa, IfcPlate según orientación).
- ⬜ **Conversión geométrica y de propiedades** `[#76]`: `ifcToPortico.js` + `ifcGeometrySimplifier.js` — eje inicial/final de cada barra, crear nodos, **snap** por tolerancia (inicial 0.01 m), unidades→m, ejes globales Z vertical; curvas/complejas → segmentos rectos o advertencia. **Materiales**: leer IFC o crear genérico con aviso (no bloquear). **Secciones**: reconocer rect/círculo/perfiles simples o aproximar por *bounding box* o genérica con aviso (no bloquear). `ifcWarnings.js` centraliza advertencias.
- ⬜ **Ventana flotante + side-by-side + confirmación** `[#77]`: `ifcImportDialog.js` (resumen del archivo, conteos por tipo, tabla con importar sí/no · tipo · nombre · nivel · material · sección · estado · advertencias; filtros) + `ifcSideBySidePreview.js` (IFC seleccionado a la izquierda vs PÓRTICO convertido a la derecha, cámaras sincronizadas, colores por estado). Robusto (no falla con no-soportados), **no modifica el modelo hasta confirmar**, integrado al **undo/redo**. Entrada en **Archivo → «Importar IFC…»**.

## G20 · UX de edición, mallado y arranque ✅ *(completo; #82 deja la pestaña «Temporales» para G7)*
*Fricciones y oportunidades observadas usando el mallador y la edición de nodos; varias son quick-wins de interacción.*
- ✅ **Mallado: invertir el flujo (botón primero, luego nodos)** `[#78]`: nuevo **modo «Mallar»** (botón en la barra lateral + menú Editar) `viewport.startMeshPick('free'|'panel')`: se activa el modo y **luego** se clican los nodos del contorno (resaltados), **Enter** malla / **Esc** reinicia, igual que el modo Área. «panel» se cierra solo al 4º nodo (transfinito); «free» acepta ≥3 (región libre). Si ya había nodos seleccionados, se arrastran al contorno (no se pierde el atajo). `mallarPanelSeleccion`/`mallarRegionLibre` aceptan ahora `ids` opcional. Verificado en navegador.
- ✅ **Arrastrar nodos con el mouse — convertido en funcionalidad** `[#79]`: arrastre robusto en modo selección con (a) **selector de plano** XY/XZ/YZ (combo en el overlay; el nodo se mueve sólo en ese plano) y (b) **candado «🔒 Fijar»** que impide moverlo sin querer; el arrastre **no se arma en modo resultados** (evita el «nodo fantasma» sobre la deformada) y el nodo **queda seleccionado y con malla viva** tras moverlo (no huérfano no seleccionable). Verificado en navegador: arrastre en XZ → Y fijo, nodo (0,0,0)→(2.5,0,2.5) con snap, sigue seleccionable.
- ✅ **Selección por cuadro (rubber-band)** `[#80]`: **Alt + arrastrar** en modo selección dibuja un recuadro (div DOM) y al soltar selecciona los **nodos** (por su centro) y **elementos** (ambos extremos dentro) que caen en él; **Ctrl** añade a la selección actual; Esc cancela. Verificado en navegador (la caja captura exactamente los nodos contenidos).
- ✅ **Nodos de malla más pequeños** `[#81]`: los nodos «de malla» (usados por áreas pero por **ninguna barra**) se dibujan con radio **0.55·NODE_R** (vs los nodos de modelo a NODE_R) para no saturar la vista; `viewport._recomputeMeshNodeSet(model)` clasifica por conectividad en cada `renderModel` (sin flag ni persistencia; sobrevive a guardar/cargar). Verificado en navegador: nodo de barra r=0.12, nodo sólo-área r=0.066.
- 🟡 **Limpiar temporales + pestaña «Temporales»** `[#82]` *(limpiar/listar hecho; falta integrar con G7)*: **Archivo → «🧹 Autoguardados temporales…»** (`manageAutosavesDialog`) lista los slots `portico_autosave_*` (marca la sesión actual), permite **eliminar** cada uno, **reanudar** uno (con confirmación, descarta el trabajo actual) o **«Limpiar todos»**; helper compartido `_deleteAutosave(key, legacy)`. Verificado en navegador. *(Pendiente: integrarlo como **pestaña «Temporales»** dentro de la gestión multi-modelo `G7`.)*
- ✅ **Auto-cierre de la ventana de inicio / recuperación** `[#83]`: tras **~8 s sin interacción**, la **portada/landing** (IIFE en `index.html`) y el **diálogo de «recuperar autoguardado»** (`_autosaveRecoveryDialog`) se **cierran solos** y pasan a un **modelo nuevo** vacío; cualquier interacción (pointer/teclado/wheel/touch) reinicia el contador, y si el **manual** está abierto encima se pospone. No bloquea el arranque.
- ✅ **Limitar dimensiones en el diseño (ancho/alto máx.)** `[#84]`: `seleccionarPerfil` acepta `prefs.maxWidth`/`maxHeight` (m) y **excluye** los candidatos que no caben **antes** de verificar (helper `sectionWH` por forma: I→bf/d, box→b/h, pipe→D, rect/otros→b/h); el overlay de auto-diseño tiene inputs **«ancho máx / alto máx (cm)»** que recalculan en vivo. Distingue claramente «**ninguno por dimensiones**» de «ninguno por resistencia» y reporta cuántos se excluyeron (sin inventar, coherente con la regla central de G17). Verificado en navegador: ancho≤12 cm → IPE360 descartado y nota con N excluidos; ancho≤4 cm → sin solución por dimensiones.

---

## Secuencia sugerida — qué sigue, de lo MÁS FÁCIL a lo más difícil

*El grueso histórico (G1–G8, G10, G11, G15) está hecho; G12 lineal cerrado. Lo abierto, ordenado por
esfuerzo creciente (incluye los **pendientes normativos** de G16):*

**🟢 Fáciles (sin código nuevo o cambio mínimo y acotado)**
1. **Materializar más verificaciones** (G9) con el pipeline ya armado — **sin código nuevo**, sólo construir `.s3d` + manifiesto. Siguientes: `1-001` (cargas generales), `1-007` (releases), `1-002` (térmica). *(El runner de espectro habilita después los edificios `1-020`/`1-024`.)* **← SIGUIENTE.** Ahora también pueden materializarse `1-005` (settlement, `[#54]`), `1-012` (`[#56]`), `1-017` (`[#55]`), `2-014` (`[#57]`), `3-002/3/4` (`[#58]`).
2. ✅ **`[#56]` Miembros compression-only** — hecho (ver G14).
3. ✅ **`[#54]` Desplazamiento prescrito de nodo/apoyo** — hecho (ver G14).

**🟡 Medias (reusan motores existentes)**
4. ✅ **`[#55]` Modal con rigidez geométrica (Kg)** — hecho (ver G14).
5. ✅ **`[#57]` Gradiente térmico en el espesor de áreas** — hecho (ver G14).
6. ✅ **`[#58]` Tensión/deformación plana** en membrana — hecho (ver G14).

**🔴 Mayores (cambios de flujo o features grandes)**
7. **`[#48b]`** time-history **no lineal** incremental (G12) · **`[#85]`** time-history con cáscaras NL vía **barras equivalentes** simplificadas (G12) · **`[#86]`** **macromodelos** (infill wall, etc.) como objeto 2D que expande a barras/cables/resortes (G13) · ✅ **`[#59]`/`[#60]`/`[#61]`** puentes (etapas, pretensado, cargas móviles) — **motores hechos y verificados** (1-031/1-009/1-030); falta sólo la UI · **`[#52]`** mesheador Gmsh (G13) · **`[#53]`** IA torre (G13) · **G7** multi-modelo (rediseño mayor).

**🏛️ Pendientes normativos (G16)** — suben la fidelidad del diseño a SAP2000 sin bloquear el uso actual: `[#66]` catálogo de perfiles tabulados (fácil-medio) · `[#67]` más formas (C/L/T) · `[#64]` interacción kyy/kzz EC3 · `[#63]` aluminio (AA ADM / EC9) · `[#65]` diagrama P-M de hormigón · `[#68]` servicio/detallado sísmico por norma.

**🧰 Módulos grandes nuevos (G17–G19)** — features mayores, en orden de dependencia interna:
- **G17 secciones/diseño automático** `[#69-#73]`: empezar por la **biblioteca precargada** `[#69]` (absorbe `[#66]`), luego el **editor ampliado** `[#70]` (polígono/huecos/H.A. con barras; suma C/L/T `[#67]`), **predimensionar** `[#71]`, **diseñar por catálogo** `[#72]` (reusa G15) y la **UX/preview** `[#73]`. *(Prioridad del propio pedido: editor rect/circ/I/polígono → guardar/asignar → predimensionar → diseñar acero → madera → H.A. → reporte → ayuda.)*
- **G18 SAP2000/ETABS** `[#74]`: modelo neutro + export/import `.s2k`/`.e2k` (esfuerzo medio, sin solver nuevo).
- **G19 importador IFC** `[#75-#77]`: depende de una librería web (web-ifc); el más grande de UI; barras primero, áreas cuando G-áreas lo permita.

## Decisiones tomadas
*Todas resueltas y ya reflejadas en los grupos correspondientes.*
- ✅ **G2 (método modal)**: usar **iteración de subespacio** (Bathe). *(Implementado — ver G2.)*
- ✅ **G7 (multi-modelo)**: modelos unidos **sólo en la memoria**, sin vínculo geométrico. *(Reflejado en G7.)*
- ✅ **G9 (verificación SAP2000)**: priorizar en el orden **análisis dinámico modal → viga → pórtico → muro/shell** (el modal va primero). *(Reflejado en G9.)*
- ✅ **G9 (figuras/pipeline)**: leer las figuras de los PDF con **poppler** y batchear con un **harness headless** + figuras SVG; lo único caso-a-caso es construir el `.s3d` y validar. *(Implementado.)*
- ✅ **G12 (time-history)**: integración por **Duhamel** por modo (es lineal); registros de ejemplo **Llolleo y Constitución 2010**; excitación **uniforme en la base** (sin multi-apoyo). *(Reflejado en G12.)*
- ✅ **G6 `[#41]` (memoria por proyecto)**: migrar los datos de memoria al `.s3d` **conservando compatibilidad** con archivos viejos. *(Implementado — ver G6.)*
- ✅ **Decisión de mallado `[#52]`**: **NO** embeber Gmsh en el cliente (10–30 MB, rompe el modo offline/PWA y el ethos sin-build; su API no respeta nuestras convenciones). Se construye un **mallador propio liviano** y autónomo (verificable en Node), **QUAD-dominante**, por fases: (1) transfinito de Coons + multi-parche con soldadura, (2) calidad de malla, (3) quad libre/paving. Gmsh queda como **herramienta offline** opcional para CAD (STEP/IGES) y mallas de verificación, nunca embebido. *(Fase 1 implementada — ver G13.)*

## Decisiones tomadas para G14 *(todas implementadas — ver G14)*
- ✅ **D1 — `[#56]` compression-only**: flag por elemento `el.compressionOnly`, excluyente con `cable`, check en el panel del elemento.
- ✅ **D2 — `[#54]` desplazamiento prescrito**: propiedad del nodo (`node.prescDisp`), como las cargas nodales (en el panel del nodo).
- ✅ **D3 — `[#55]` modal con Kg**: caso/combo de referencia + toggle «incluir P-Δ (Kg)» en el diálogo modal (como SAP2000).
- ✅ **D4 — `[#57]` gradiente térmico en área**: temperatura por cara (roja +z / azul −z, como Abaqus).
- ✅ **D5 — `[#58]` tensión/deformación plana**: propiedad del área (`area.planeStrain`).

---

## ✅/⬜ FALTANTES — lista única ordenada por FACILIDAD de implementación
*Todo lo que queda ⬜/🟡 a la fecha (v155), de lo MÁS FÁCIL a lo más difícil (esfuerzo, no complejidad teórica). El detalle de cada uno está en su grupo arriba; aquí sólo lo que FALTA.*

**🟢 Triviales (constante/CSS/timeout, < ½ día)** — ✅ **ambos hechos (v154)**
1. ✅ **Nodos de malla más pequeños** `[#81]` (G20) — hecho (radio 0.55·NODE_R para nodos sólo-área; ver G20).
2. ✅ **Auto-cierre de portada / recuperación** `[#83]` (G20) — hecho (~8 s sin interacción → modelo nuevo; ver G20).

**🟢 Fáciles (cambio acotado, reusa lo existente)**
3. 🟡 **Limpiar temporales + pestaña «Temporales»** `[#82]` (G20) — **limpiar/listar hecho** (Archivo → «Autoguardados temporales…»); falta integrarlo como pestaña dentro de multi-modelo (G7).
4. ✅ **Limitar dimensiones en el diseño (ancho/alto máx.)** `[#84]` (G20) — hecho (`prefs.maxWidth/maxHeight` + inputs en el overlay; ver G20).
5. **Materializar más casos de verificación** `[#19]` (G9) — **sin código nuevo**: construir `.s3d` + manifiesto (1-001, 1-007, 1-002, etc.). Laborioso pero mecánico. **← PARA EL FINAL**
6. **Refinos finos de diseño** (G16): armado multicapa explícito en P-M biaxial + parábola-rectángulo EC2 `[#65]`; centro de cortante del canal `[#67]`; flecha relativa a la cuerda por elemento en la tabla `[#68]`. **← PARA EL FINAL**

**🟡 Medios (UI de viewport o motor acotado)** — ✅ **trío de viewport de G20 hecho (v155)**
7. ✅ **Mallado: invertir el flujo (botón → luego nodos)** `[#78]` (G20) — hecho (modo «Mallar»; ver G20).
8. ✅ **Selección por cuadro (rubber-band con Alt)** `[#80]` (G20) — hecho (Alt+arrastrar; ver G20).
9. ✅ **Arrastrar nodos con el mouse + corregir bug de nodos fantasma** `[#79]` (G20) — hecho (plano XY/XZ/YZ + candado, sin huérfanos; ver G20).
10. **Time-history modal con elementos de ÁREA** `[#51]` (G12) — verificar muro/losa end-to-end + historia de tensiones σ(t) de área.
11. **Time-history NO LINEAL — rótulas concentradas en extremos de barra** `[#48b]` (G12) — hoy es edificio de corte; falta el modelo completo con rótulas por elemento.
12. **UX de los análisis avanzados + `.md` por funcionalidad** `[#20]` (G9) — ejemplos guiados (pandeo, form-finding, pushover) y docs.
13. **UX de la pestaña Diseño (avanzada)** `[#73]` (G17) — selector de material permitido, panel de preferencias, reporte de diseño.
14. **Editor gráfico de vértices del polígono** `[#70]` (G17) — dibujar/editar el contorno con el mouse (hoy es por textarea de coordenadas).
15. **UI de nudos columna fuerte-viga débil** `[#68]` (G16) — resaltar en el viewport los nudos que no cumplen SCWB.
16. **Cacho rígido NATIVO (zona rígida de extremo)** `[#87]` (G14) — propiedad `el.rigidEnd={i,j}` que condensa un brazo rígido en cada extremo dentro de `K`/`M`/FEF y acorta la luz flexible (el «cacho» viga-columna de SAP2000/ETABS), + UI en el panel del elemento + test analítico. Hoy sólo se emula con link rígido.

**🟠 Mayores (motor/feature nuevos)**
17. **IA: torre de transmisión (celosías 3D)** `[#53]` (G13) — vocabulario de ficha + generador paramétrico en el asistente.
18. **Otras capacidades parciales** `[#G14]` — resorte de extremo (1-008), viga sobre fundación (1-013), pandeo de cáscara (2-016/2-017), viga corotacional gran rotación (1-029).
19. **Time-history con cáscaras NO LINEALES — completa y simplificada** `[#85]` (G12) — dos vías: **(A) completa** sobre la malla real (rigurosa, **más costosa**) y **(B) simplificada** condensando paneles a barra(s) corotacional(es) y devolviendo esfuerzos integrados a las áreas (rápida; implementar primero).
20. **Macromodelos (infill wall de albañilería, etc.)** `[#86]` (G13) — objeto 2D «panel» que expande a una red de barras + cables + resortes/links NL calibrados (puntal diagonal equivalente); catálogo de macromodelos.
21. **Documentación integral de toda funcionalidad** `[#21]` (G9) — qué hace, teoría mínima, cómo ejecutarla, ejemplo, por cada feature. **← PARA EL FINAL**
22. **Mesheador — paving/calidad avanzada** `[#52]` (G13) — fases 1-3 hechas; quedan refinamientos (recombinación quad avanzada, métricas).

**🔴 Grandes (módulos/arquitectura)**
23. **Interoperabilidad SAP2000 / ETABS /INP CALCULIX/ABAQUS /DAT SOFISTIK/OPENSEES/VECTOR (`.s2k`/`.e2k` / `.INP`/`.dat`)** `[#74]` (G18) — modelo neutro + import/export, verificable headless.
24. **Proyecto multi-modelo en una sola memoria** `[#17]` (G7) — serializer multi-modelo + estado de modelo activo + generador de memoria que los recorre (incluye la pestaña «Temporales» del `[#82]`).
25. **Importador IFC (web-ifc)** `[#75]`/`[#76]`/`[#77]` (G19) — el más grande: loader + clasificador + conversión + ventana side-by-side con confirmación.
