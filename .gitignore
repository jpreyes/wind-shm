node_modules/
dist/
.DS_Store
Thumbs.db
*.log
npm-debug.log*
.claude/
# data/: ignora el contenido pesado (KMZ/DEM binarios) PERO conserva los .json que
# la app necesita (p.ej. caman_dem.json — heightmap del relieve). R-40f.
data/*
!data/*.json

# Archivos del profesor (fuente local, NO se publican): Excel de normas y PDFs
excel/
# Artefacto de prueba con resultados embebidos (no se trackea)
examples/portico_simple_v3.s3d
# Temporales del asistente
asistente/_gen_tmp/
asistente/_sp001/
# Cloudflare Worker: secretos locales y caché de build
.dev.vars
.wrangler/

# Memoria de referencia (.docx): fuente local, no se versiona ni se publica
referencias/*.docx
*.docx

# Zips subidos al repo local (entregables/fuentes), no se versionan
*.zip

# Auditorías internas (notas de trabajo, no se publican)
auditoria/
